const producto = {
    //para asignar el valor se utilizan los dos puntos
    nombre : "Monitor 20 pulgadas",
    precio : 300,
    disponible : true
}

console.log(producto);

//ACCEDER A LOS VALORES DE UN OBJETO
//Los objetos tienen la sistaxis de punto
console.log(producto.nombre);
//Forma menos común
console.log(producto['nombre']);