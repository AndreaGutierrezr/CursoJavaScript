const cadena = 'Monitor 20 pulgadas';
console.log(cadena.toUpperCase()); //convierte a mayúsculas
console.log(cadena.toLowerCase()); //convierte a minúsculas

//convierte numeros en cadena
const precio = 300;
console.log(precio);
console.log(precio.toString());